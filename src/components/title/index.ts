import styled from '@emotion/styled';

export const AsideTitle = styled.h2({
  fontWeight: 'bold',
  fontSize: '1.2em',
  alignSelf: 'center',
  marginBottom: '5px',
});

export const SectionsTitle = styled.h2({});

export const ColsTitle = styled.h3({});
